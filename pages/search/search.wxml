<input class="search" confirm-type="search" bindconfirm="confirmSearch" placeholder="搜索目的地" />

<!-- 返回查询结果 -->
<view wx:for="{{array}}" wx:key="key">
  <view class="list" bindtap="getParkingInfoDetail" data-id="{{item.pid}}">
    <view class="item">{{item.name}}</view>
  </view>
</view>
